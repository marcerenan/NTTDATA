info:
  name: GetCustomers
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8081/api/v1/customers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
