info:
  name: AddAccount
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8082/api/v1/accounts
  body:
    type: json
    data: |2-
        {
        "accountNumber": "33325224",
        "accountType": "Checking",
        "initialBalance": 100.0,
        "customerId": 1,
        "status": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
