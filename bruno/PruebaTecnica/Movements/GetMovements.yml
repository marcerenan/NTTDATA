info:
  name: GetMovements
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8082/api/v1/movements
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
